# Core HTTP/API runtime. Playwright is intentionally optional.
Flask>=2.2
requests>=2.31
beautifulsoup4>=4.12
tqdm>=4.66

# Browser compatibility engine:
#   pip install -e ".[playwright]"
#   python -m playwright install chromium

# Authenticated phone/LAN access with a production WSGI server and QR pairing:
#   pip install -e ".[mobile]"
